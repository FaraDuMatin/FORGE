# Neon Postgres connection string (used everywhere — no SQLite).
# Local dev: a Neon dev branch. Prod: the Neon prod branch, set in Vercel env.
# Get one free at https://neon.tech, then copy the pooled connection string here.
DATABASE_URL="postgresql://USER:PASSWORD@HOST/DB?sslmode=require"
